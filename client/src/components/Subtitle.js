import styled from 'styled-components';

const Subtitle = styled.div`
  font-size: 15px;
  font-weight: bold;
  margin-bottom: 5px;
  p {
    margin: 0;
    color: #555;
    font-size: 12px;
    font-weight: normal;
  }
`;

export default Subtitle;
